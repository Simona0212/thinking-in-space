# 快速修复 - 所有模型错误

## 🔴 三个错误

1. **Qwen Instruct**: "Incorrect format used for video"
2. **Qwen Thinking**: "AutoModelForImageTextToText not found"
3. **LLaVA**: "torch_dtype is deprecated"

## ✅ 一键修复（复制粘贴）

```bash
# 安装缺失依赖
pip install qwen-vl-utils einops timm av pillow
```

## 🔧 我已经修复的代码

### 1. Qwen视频处理 - 3层fallback
- ✅ 优先使用`qwen-vl-utils.process_vision_info`
- ✅ Fallback到`file://`格式
- ✅ 最后fallback到帧提取

### 2. Qwen类导入 - 已正确
- ✅ Qwen3VLEvaluator对Instruct和Thinking都有效
- ✅ 多层import fallback已实现

### 3. LLaVA参数兼容
- ✅ 自动尝试`dtype`（新版）
- ✅ Fallback到`torch_dtype`（旧版）

## 🧪 验证

```bash
# 测试Qwen
python evaluate_vsibench.py \
    --model_name "Qwen/Qwen3-VL-4B-Instruct" \
    --gpu_id 0 \
    --limit 3

# 如果成功，运行完整评估
./run_parallel.sh
```

## ⚠️ 如果qwen-vl-utils安装失败

```bash
# 方法1：不安装依赖
pip install qwen-vl-utils --no-deps

# 方法2：从源码
pip install git+https://github.com/QwenLM/Qwen-VL.git#subdirectory=qwen-vl-utils
```

**注意**: 即使qwen-vl-utils安装失败，代码也会自动fallback到帧提取方法。

## 💡 如果LLaVA还是失败

暂时跳过，编辑`run_parallel.sh`:

```bash
MODELS=(
    "Qwen/Qwen3-VL-4B-Instruct"
    "Qwen/Qwen3-VL-4B-Thinking"
    "Qwen/Qwen3-VL-8B-Instruct"
    "Qwen/Qwen3-VL-8B-Thinking"
    # "lmms-lab/LLaVA-OneVision-1.5-4B-Instruct"  # 跳过
)
```

## 🎯 成功标志

不再看到：
- ❌ "Incorrect format used for video"
- ❌ "AutoModelForImageTextToText"
- ❌ "torch_dtype is deprecated"

而是看到：
- ✓ "Processing sample X: Success"

---

详细说明见：`所有模型错误综合修复.md`
